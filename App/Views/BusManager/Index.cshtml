@model IEnumerable<App.Models.Manager.BusViewModel>;
@{
    ViewData["Title"] = "Bus Table";
}

<h2>Bus Table</h2>
<a asp-controller="Home" asp-action="Index" class="pb-3 link-info">Back to Menu</a>
<div class="flex flex-col justify-content-center border border-secondary p-5 m-5 text-center">
    <table class="table table-striped border border-light">
        <thead>
            <tr>
                <th class="text-light">
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th class="text-light">
                    @Html.DisplayNameFor(model => model.BusNumber)
                </th>
                <th class="text-light"></th>
            </tr>
        </thead>
        <tbody>
            @if(Model != null)
            {
                @foreach(var item in Model)
                {
                    <tr>
                        <th class="text-light">
                            @Html.DisplayFor(model => item.Id)
                        </th>
                        <td class="text-light">
                            @Html.DisplayFor(model => item.BusNumber)
                        </td>
                        <td class="text-light">
                            <a asp-action="EditBus" asp-route-id="@item.Id" class="link-info">Edit</a> |
                            <a asp-action="DeleteBus" asp-route-id="@item.Id" class="link-info">Delete</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
    <a asp-action="CreateBus" class="btn btn-primary btn-lg w-25 text-light">
        Create Bus
    </a>
</div>
