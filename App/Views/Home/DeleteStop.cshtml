@model App.Models.DeleteStopModel
@{
    ViewData["Title"] = "Stop Remover";
}
<div class="container">
    <form asp-action="DeleteStop">
        <input type="hidden" asp-for="Id">
        <h2>Are you sure you want to delete this stop?</h2>
        <div class="form-group">
            <button input="submit"value="Yes" class="btn btn-primary">
        </div>
        <div class="form-group">
            <a asp-action="Index">No</a>
        </div>
    </form>
</div>
